const superHeros: string[] = [];
const heroPower: Array<number> = [];

superHeros.push("Hello");
heroPower.push(400);

// Array of our Custom Data Type
type User = {
    name: string,
    isActive: boolean
}

const allUsers: User[] = [];

allUsers.push({ name: "", isActive: true });
export {};